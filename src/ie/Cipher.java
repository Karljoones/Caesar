package ie;

public class Cipher {
	public static String encode(String original, String key){
		
		// Variables
		String encoded;
		
		int Key = Integer.parseInt(key);
		Key = Key % 26 + 26;
		
		return encoded;
	}
}
